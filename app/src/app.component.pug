div(class="bgd-zeta header")
  div(class="middle")
    div(class="row header-container")
      div(class="col6")
        h1(class="header-title")
          strong Angular 2
          |  Seed
        h2(class="header-subtitle") A basic Angular 2 TypeScript starter project
      div(class="col6")
        ul(class="navmenu align-right header-nav")
          li(*ngIf="!isUserAuthorized")
            a([routerLink]="['/']", [class.active]!="'/' | isRouteActive") Home
          li(*ngIf="!isUserAuthorized")
            a([routerLink]="['/login']", [class.active]!="'/login' | isRouteActive") Login
          li(*ngIf="isUserAuthorized")
            a((click)="logout()", class="pointer") Logout

router-outlet

div(class="footer-dock")
div(class="footer bgd-alpha")
  div(class="middle")
    div(class="row")
      div(class="col12")
        h2(class="clr-epsilon") About
        p
          | This project is developed by 
          a(href="http://matthiasschuetz.com/en") Matthias Schuetz
          |  and the source code is available on 
          a(href="https://github.com/matthias-schuetz/angular2-seed/") GitHub
          | . This sample app also uses the 
          a(href="http://coresheet.net") CoreSheet CSS framework
          |  for visualization.